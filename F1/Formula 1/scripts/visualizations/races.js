/**
 * Constructors visualization placeholder
 */
function initRacesVisualization(selector, data, driversData) { //change params as required; literally just a placeholder

    const container = d3.select(selector);
    container.html("");
    container.append("div")
        .attr("class", "placeholder-message")
        .text("Constructors visualization will be implemented according to design");
}